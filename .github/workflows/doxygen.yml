name: Doxygen

on:
  push:
  pull_request:
  workflow_dispatch:

jobs:
  build:
    runs-on: ubuntu-latest
    steps:
      - name: "Setup git"
        run: |
          git config --global user.name "${{ github.actor }}"
          git config --global user.email "${{ github.actor }}@users.noreply.github.com"

      - uses: actions/checkout@v2

      - name: "Setup cmake"
        run: cmake --version

      - name: "Setup doxygen"
        run: |
          sudo apt-get -y install doxygen graphviz
          doxygen --version
          dot -V

      - name: "Change directory: sample"
        run: cd sample

      - name: "Setup gil"
        run: |
          pip3 install gil
          gil update

      - name: "Change directory: build/Unix"
        run: cd build/Unix

      - name: "Doxygen"
        env:
          GITHUB_ACTOR: ${{ github.actor }}
          GITHUB_TOKEN: ${{ secrets.GITHUB_TOKEN }}
        run: |
          ./01-generate.sh
          ./05-doxygen.sh
